# Running the app

The Backend currently exposes the API. A later deployment/static-host step should serve `Mobile/app` from the same origin or provide a proxy for `/platform`.

No fake remote API is embedded in the app.
