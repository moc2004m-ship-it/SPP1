# Stage 1 — TODO المؤجّل (يحتاج Internet / Server / حساب خارجي)

# Stage 1 — TODO المؤجّل

هذا الملف يجمّع **كل بند** من Stage 1 تعذّر إكماله بالكامل داخل بيئة
الملفات فقط. لا شيء هنا اعتُبر "منتهي" أو خطأ في المشروع — كل بند مؤجَّل
بوضوح لسببه الحقيقي، ومُصنَّف إلى واحدة من فئتين مختلفتين تمامًا:

---

## الفئة أ) محتاج اتصال إنترنت فقط (مش حساب/خدمة خارجية)

### `Backend/package-lock.json`
- **الحالة:** غير موجود، ولم يُنشأ، ولن يُخترع بأرقام/hashes وهمية.
- **السبب:** توليده يتطلب طلب فعلي لـ `registry.npmjs.org` لجلب الإصدارات
  الدقيقة وقيم الـ integrity الحقيقية. اتأكدت فعليًا إن بيئة التنفيذ الحالية
  بلا إنترنت (`npm install --package-lock-only` رجع `403 Forbidden`).
- **الأثر:** خطوة `npm ci` في `.github/workflows/ci-cd.yml` ستفشل لحد ما
  الملف ده يتولّد بتشغيل حقيقي لـ `npm install` على أي بيئة متصلة بالإنترنت
  (جهازك، أو GitHub Actions نفسها)، والملف الناتج يترفع للمشروع.
- **ملاحظة:** هذا البند لا يحتاج حساب أو اشتراك في أي خدمة — يحتاج وصول
  شبكة فقط.

---

## الفئة ب) [External Setup — Deferred] — محتاج حساب/خدمة خارجية فعلية

### 1. النشر الفعلي إلى Staging
- **الملف:** `.github/workflows/ci-cd.yml` → job `deploy-staging`.
- **الحالة الحالية:** الخطوة مُسمّاة صراحة
  `[External Setup — Deferred] Deploy to Staging` وتطبع رسالة توضيحية بدل ما
  تنفّذ نشرًا فعليًا — بحيث ما حدش يفتكرها نشر حقيقي شغال.
- **المطلوب لاحقًا:** حساب Cloud provider حقيقي (Render/Fly.io/غيره)، ربط
  الـ repo به، وإضافة أسرار GitHub حقيقية (`CLOUD_API_TOKEN`,
  `STAGING_APP_NAME`, أو `RENDER_DEPLOY_HOOK_URL` حسب المزوّد).

### 2. Log Drain حقيقي (تجميع مركزي)
- **الملف:** `Backend/src/logger.js`.
- **الحالة الحالية:** الكود **جاهز وشغّال فعليًا** — بيبعت أي log لـ
  `LOG_DRAIN_URL` لو القيمة دي متظبطة، وبيتجاهل أي فشل اتصال من غير ما يكسر
  التطبيق. مفيش أي تعديل كود مطلوب لاحقًا.
- **المطلوب لاحقًا:** حساب فعلي بخدمة تجميع logs (Better Stack/Logtail/إلخ)
  والحصول على `LOG_DRAIN_URL` حقيقي ووضعه كمتغير بيئة في Staging/Production.

### 3. حساب Render/Cloud وربط الـ repo
- لم يُنشأ ولن يُفترض وجوده. مؤجَّل بالكامل لمرحلة النشر الفعلي.

---

**ملخص:** الفئة (أ) هي البلوكر الوحيد اللي بيمنع تشغيل الـ CI فعليًا لحد
تشغيل `npm install` مرة واحدة بإنترنت. الفئة (ب) بالكامل [External Setup —
Deferred] — كود جاهز 100%، وناقصها بس قرار/حساب خارجي منك في مرحلة لاحقة.
