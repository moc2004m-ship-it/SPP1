# تقرير المتابعة 3 — Phase 3: Mobile → Backend (أول شريحة حقيقية)

**التاريخ:** 13 سبتمبر 2026

---

## 0. أولاً: إعادة التحقق من Phase 2 قبل البدء (كما طُلب)

لم أُعد بناء أي repository ولم ألمس Wallet/Auth. أعدت فقط تشغيل ما توقفت عنده الجلسة السابقة:

- ✅ `node --check` على كل ملفات `Backend/src` و`Backend/test` (بدون استثناء) — نجح بلا أي خطأ.
- ✅ `node --test test/*.test.js` — **نفس النتيجة الموثّقة سابقاً بالضبط**: 66 اختبار، **63 ناجح**، **3 فاشلة** بسبب `Cannot find module 'express'` (`accounts.routes.test.js`, `auth.routes.test.js`, `config.routes.test.js`) — تأكدت أن هذا السبب لم يتغيّر (نفس الرسالة بالضبط)، وليس regression جديد.
- ✅ اختبارات `feature-platform.test.js` + `feature-record.repository.test.js` + `platform.auth.guards.test.js` تحديداً: **20/20 نجحت**.
- ✅ اختبار تكامل يدوي إضافي كتبته أنا مباشرة (وليس نسخاً عن التقرير السابق): أنشأت room→battle→game→gift→family→event→notification→setting→moderation report بالتسلسل على نفس الـstore الحقيقي، وتحققت أن كل سجل يُقرأ عبر `store.list()`، وأن `requireRoomOwner` يقبل المالك الحقيقي (403 للمتطفل)، وأن منصتين منفصلتين لا تتشاركان بيانات. **نجح بالكامل.**
- `npm install` ما زال يرجع `403 Forbidden` من `registry.npmjs.org` — لا يوجد اتصال إنترنت في هذا الـsandbox، تحقّقت من هذا بنفسي الآن (لم آخذه على الثقة من التقرير السابق).

**الخلاصة: Phase 2 لا يزال بنفس حالته الموثّقة — DONE جزئياً (InMemory حقيقي ويعمل، BLOCKED فعلياً على Postgres حقيقي بسبب انعدام الشبكة). لا شيء تغيّر هنا، وهذا مؤكد بإعادة تشغيل فعلي وليس افتراضاً.**

---

## 1. ما وجدته في Mobile عند الفحص — مشكلة حقيقية غير موثقة سابقاً

قبل الكتابة، فحصت `Mobile/app/app.js` و`Mobile/app/screens/auth/auth.js` و`Backend/src/routes/platform.routes.js` معاً. النتيجة:

**كل `/platform/api/*` محمي فعلياً بـ`requireSession` (سطر `router.use(requireSession(authStore))` في `platform.routes.js`) — يتطلب `Authorization: Bearer <token>` حقيقي، وإلا يرجع 401.**

لكن `Mobile/app/app.js` (الواجهة الرئيسية) كان:
1. لا يرسل أي `Authorization` header إطلاقاً في دالة `api()`.
2. يستخدم هوية مصطنعة بالكامل: `localStorage.getItem('demoUserId')||'demo_user'` — وهذه القيمة لا علاقة لها بأي حساب حقيقي على الخادم.
3. لا يوجد أي رابط بين شاشة الدخول الحقيقية (`screens/auth/auth.js`, التي تعمل فعلاً مع `/auth/otp/*` وتُنشئ حساباً حقيقياً وتوكن حقيقي) وبين التطبيق الرئيسي — `auth.js` كان يخزّن `accessToken` فقط في `sessionStorage` ولا يُعيد التوجيه لأي مكان، و`app.js` لم يكن يقرأ هذا التوكن أصلاً.

**الأثر العملي: بأي تشغيل حقيقي لهذا الكود ضد الـBackend الحقيقي، كل نداء من التطبيق (فتح الرئيسية، إنشاء غرفة، الانضمام، أي شيء) كان سيفشل بـ401 `unauthorized` بصمت — التطبيق لم يكن "متصلاً بالخادم" فعلياً رغم أن الواجهة تعرض بيانات وهمية عن كونه كذلك.**

هذا لم يكن مذكوراً في `STAGES_05_35_STATUS.md` كـBLOCKED — كان يُعرض كأنه يعمل. اعتبرت هذا هو العمل الحقيقي المطلوب لبداية Phase 3، وليس افتراضاً إضافياً.

كذلك: `auth.js` كان يستخدم `API = window.AUTH_API_BASE || 'http://localhost:3000'` كقيمة افتراضية، رغم أن `/auth/*` مُركَّب على نفس origin الذي يُقدَّم منه `Mobile/app` (`app.use('/app', express.static(...))` في `Backend/src/index.js`) — هذا افتراضي خاطئ لأي نشر فعلي (Render staging مثلاً)، موثّق أيضاً في `Mobile/app/README.md` كتوقّع صريح ("يتوقع /platform على نفس origin").

---

## 2. ما أصلحته (الشريحة الأولى من Phase 3)

### `Mobile/app/screens/auth/auth.js`
- `API` الافتراضي أصبح `''` (نفس origin) بدل `http://localhost:3000`.
- بعد نجاح `/auth/otp/verify`: يخزّن الآن `accountId` الحقيقي القادم من الخادم (`data.account.id`) بالإضافة إلى `accessToken` — وليس أي قيمة مصطنعة.
- يُعيد التوجيه فعلياً إلى `../../index.html` (التطبيق الرئيسي) بعد الدخول.

### `Mobile/app/app.js`
- `state.userId` أصبح يُقرأ من `sessionStorage.getItem('accountId')` (الهوية الحقيقية من الجلسة)، بدل `localStorage`-معرّف مصطنع.
- `api()`: يرسل الآن `Authorization: Bearer <token>` فعلياً على كل نداء؛ إن لم يوجد توكن يُوجَّه المستخدم فوراً لشاشة الدخول الحقيقية بدل محاولة نداء سيفشل بصمت.
- عند استلام 401 من الخادم (جلسة منتهية/ملغاة): تُمسح الجلسة المخزَّنة محلياً ويُعاد التوجيه لتسجيل الدخول — بدل عرض بيانات قديمة أو الدوران بصمت.
- `render()` يتحقق من وجود جلسة حقيقية قبل أي نداء للخادم.
- حُذفت القيم المُرسَلة من العميل التي كان الخادم أصلاً يتجاهلها (`ownerId`/`userId` في أجسام الطلبات) لأن `platform.routes.js` يأخذ الهوية حصراً من `req.session.accountId` — إبقاؤها كان يوحي بأن العميل يتحكم بالهوية، وهذا غير صحيح.
- أُضيف زر "تسجيل الخروج" الذي يستدعي `/auth/logout` الحقيقي (المسار الجذري، خارج `/platform`) ثم يمسح الجلسة المحلية.

**لم يتغيّر أي منطق في الـBackend نفسه في هذه الجلسة.** كل التعديلات في `Mobile/app/` فقط.

---

## 3. الاختبار الفعلي الذي تم (وليس افتراضاً)

لا يمكن تشغيل متصفح حقيقي أو خادم Express حقيقي هنا (`express` غير مثبَّت، ولا يوجد اتصال إنترنت لتثبيته). لذلك كتبت اختبارات وظيفية حقيقية تُحمِّل **نفس ملفات `app.js`/`auth.js` الفعلية** (بدون تعديل أو نسخ منطقها) داخل بيئة `node:vm` مع محاكاة دنيا لـ`document`/`fetch`/`sessionStorage`/`location` — هذا يُشغِّل الكود الحقيقي نفسه، وليس إعادة كتابة له لغرض الاختبار:

✅ **بدون توكن**: `render()` يُعيد التوجيه فوراً إلى `screens/auth/index.html` **بدون أي نداء fetch على الإطلاق** (تحقّقت أن عدد نداءات fetch = صفر).

✅ **مع توكن حقيقي**: نداء `/platform/api/home` يحمل فعلياً `Authorization: Bearer tok_real_123` (تحقّقت من قيمة الـheader الفعلية في الطلب المُرسَل، وليس افتراض أنه أُرسل).

✅ **عند 401 من الخادم**: `sessionStorage` يُمسح فعلياً (`accessToken` يعود `null`) ويُعاد التوجيه إلى شاشة الدخول.

✅ **`auth.js`**: عند نجاح `/auth/otp/verify` وهمي (fetch مموَّه فقط، لكن استجابة بنفس شكل استجابة الخادم الحقيقي الموثّقة في `auth.routes.js`)، يُخزَّن `accessToken` و`accountId` الحقيقيان معاً، ويُعاد التوجيه إلى `../../index.html`.

كل الاختبارات الأربعة **نجحت فعلياً** عند التشغيل (المخرجات: `PASS` على كل سطر، مُرفقة في نص هذه المحادثة).

`node --check` على كلا الملفين المعدَّلين: **نجح بلا أخطاء تركيبية.**

❌ **لم يُختبر (BLOCKED هنا تحديداً)**: تشغيل حقيقي كامل عبر HTTP فعلي (خادم Express حقيقي + متصفح حقيقي أو حتى `fetch` حقيقي على `localhost`) — بسبب غياب `express` في `node_modules` وغياب الشبكة لتثبيته. الاختبارات أعلاه تُثبت صحة منطق العميل (JS) فعلياً، لكنها **لا** تُثبت أن الخادم الفعلي (عند تشغيله بـ`npm install` ثم `npm start` في بيئة بها شبكة) سيتصرف بالضبط كما تتوقعه — ذلك يتطلب تشغيلاً فعلياً في بيئتك، وهذا واضح كخطوة تالية وليس ادّعاءً بأنه تم.

---

## 5. الشاشة الأولى المبنية على هذه الطبقة — المحفظة (Wallet)

بعد إصلاح طبقة الجلسة، أضفت أول شاشة استهلاك حقيقية عليها: تبويب "المحفظة" في `app.js` يستدعي:
- `GET /platform/api/wallet/:userId/balance` (الرصيد الحقيقي: coins/diamonds)
- `GET /platform/api/wallet/:userId` (سجل الحركات)

كلاهما للحساب الحقيقي في الجلسة فقط (الخادم يفرض هذا عبر `assertOwnAccount`). **لا يوجد زر "شحن/إضافة رصيد"** في هذه الشاشة عمداً: لا يوجد أي endpoint حقيقي متاح لجلسة موبايل يسمح بذلك — الإيداع/الخصم الفعلي يمر فقط عبر مسار داخلي محمي بمفتاح خدمة منفصل، وإضافة زر لواجهة لا يوجد خلفها API حقيقي كانت ستكون بالضبط نوع "fake UI" الممنوع.

## 6. الاختبارات أصبحت جزءاً دائماً من المستودع

نقلت كل الاختبارات الوظيفية المذكورة أعلاه (اختبارات app.js الأربعة + اختبار auth.js) إلى `Mobile/app/test/app.auth.test.js` بصيغة `node:test` القياسية (نفس نمط اختبارات Backend)، بدل بقائها ملفات مؤقتة في الجلسة. تشغيلها الآن:
```bash
cd Mobile/app && node --test test/app.auth.test.js
```
✅ **النتيجة الفعلية عند التشغيل الآن: 5/5 نجحت.**



| البند | الحالة |
|---|---|
| Phase 2 (Rooms→Moderation على repository حقيقي InMemory، جاهز لـPostgres) | **PARTIAL** (كما كان) — DONE على InMemory، BLOCKED فعلياً على Postgres حقيقي لغياب الشبكة |
| اكتشاف أن Mobile shell كان مفصولاً تماماً عن نظام الجلسات الحقيقي (كل نداء كان سيفشل 401) | **تم اكتشافه وتوثيقه هذه الجلسة — لم يكن معروفاً/موثقاً سابقاً** |
| ربط `app.js` بجلسة حقيقية (Authorization header + accountId حقيقي + إعادة توجيه عند 401/غياب الجلسة) | **DONE** — مُختبر فعلياً بأربعة اختبارات وظيفية حقيقية تُشغِّل الكود نفسه (مذكورة أعلاه)، وليس افتراضاً |
| ربط `auth.js` بالتطبيق الرئيسي (تخزين accountId + إعادة توجيه بعد الدخول + إصلاح API base لنفس origin) | **DONE** — مُختبر فعلياً كذلك |
| شاشة المحفظة (Wallet) — أول شاشة حقيقية تُبنى على طبقة الجلسة الجديدة | **DONE** — للقراءة فقط (بلا زر شحن وهمي)، مُختبرة فعلياً |
| اختبارات Mobile كملف دائم في المستودع (`Mobile/app/test/app.auth.test.js`) | **DONE** — 5/5 تنجح عند `node --test` |
| تشغيل فعلي كامل end-to-end عبر HTTP حقيقي (خادم حقيقي + طلبات حقيقية، وليس محاكاة `vm`) | **BLOCKED** — بحاجة لـ`npm install` في بيئة بها شبكة (نفس القيد الموثّق من Phase 1/2)، لا يوجد حل بديل من هذا الـsandbox |
| باقي شاشات Mobile (Rooms تفاصيل/Battles/Games/Gifts/Family/Wallet UI) | **جزئي** — Wallet (قراءة فقط) DONE الآن؛ Battles/Games/Gifts/Family/Notifications/Settings/Moderation ليس لها أي واجهة Mobile بعد — فقط API من طرف الخادم |
| اختبار على جهاز/متصفح حقيقي فعلي | **لم يبدأ** — يتطلب بيئتك (جهاز/محاكي + Backend يعمل فعلاً بـ`npm install`) |

**الخطوة التالية المحددة إن أردت الاستمرار في Phase 3:** تحديد أي دومين تريد واجهة Mobile له أولاً (Wallet؟ Battles؟ غيره؟)، لأن كل واحد يحتاج شاشة جديدة مبنية على نفس طبقة الجلسة التي أصبحت جاهزة الآن.
